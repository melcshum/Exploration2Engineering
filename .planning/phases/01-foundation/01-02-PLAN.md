---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - book/mkdocs.yml
autonomous: false
requirements:
  - BUILD-01
  - BUILD-03
must_haves:
  truths:
    - "mkdocs.yml has docs_dir, edit_url, site_url, extra.homepage, and nav configured correctly"
    - "mkdocs build --strict succeeds with no warnings or errors"
    - "mkdocs serve runs and serves the documentation site"
  artifacts:
    - path: "book/mkdocs.yml"
      provides: "MkDocs site configuration"
      contains: "docs_dir: book/docs"
    - path: "book/site/"
      provides: "mkdocs build output directory"
      state: "generated"
  key_links:
    - from: "book/mkdocs.yml"
      to: "book/docs/"
      via: "docs_dir configuration"
      pattern: "docs_dir: book/docs"
    - from: "mkdocs serve"
      to: "localhost:8000"
      via: "local dev server"
      pattern: "localhost:8000"
---

<objective>
Update mkdocs.yml with docs_dir, edit_url, site_url, extra.homepage, and nav configuration per locked decisions D-04, D-05, D-06, D-07. Then validate BUILD-01 (strict build) and BUILD-03 (serve).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@book/mkdocs.yml
@book/docs/ch1-ai-supported-software.md
@book/docs/index.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Get GitHub repository URL for edit_url and site_url</name>
  <decision>GitHub repository URL needed for D-05 (edit_url) and D-06 (site_url/homepage)</decision>
  <context>
    Per D-05: edit_url must be set to https://github.com/{org}/{repo}/edit/main/book/docs/{path}
    Per D-06: site_url and extra.homepage must be updated from placeholder "yourusername"

    The git remote has no URL configured. We need the actual GitHub repository URL to complete the mkdocs.yml configuration.
  </context>
  <options>
    <option id="option-a">
      <name>Provide GitHub URL now</name>
      <description>User provides the actual GitHub repository URL (e.g., https://github.com/username/exploration-to-engineering)</description>
      <action>User types or pastes the GitHub repository URL in the chat</action>
    </option>
    <option id="option-b">
      <name>Use placeholder for now</name>
      <description>Use "yourusername/exploration-to-engineering" as placeholder and mark edit_url as deferred for Phase 2 when repo is known</description>
      <action>mkdocs.yml updated with placeholder URL; edit_url points to non-functional URL but build succeeds</action>
    </option>
  </options>
  <resume-signal>Select option-a (provide URL now) or option-b (use placeholder) or provide the GitHub URL directly</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update mkdocs.yml configuration</name>
  <files>book/mkdocs.yml</files>
  <read_first>
    - book/mkdocs.yml (current configuration that needs updates)
  </read_first>
  <action>
    Update book/mkdocs.yml with the following changes per D-04, D-05, D-06, D-07:

    1. Add `docs_dir: book/docs` — sets the documentation source root (D-04)

    2. Update `site_url` from `https://yourusername.github.io/exploration-to-engineering` to the actual repository URL converted to Pages format (e.g., `https://username.github.io/exploration-to-engineering`) (D-06)

    3. Update `extra.homepage` from `https://github.com/yourusername/exploration-to-engineering` to the actual GitHub repository URL (D-06)

    4. Update `extra.social[0].link` from `https://github.com/yourusername/exploration-to-engineering` to the actual GitHub repository URL (D-06)

    5. Set `edit_url` to point to the GitHub repository for editing (D-05). If option-a was selected, use the actual URL. If option-b was selected, use a placeholder.

    Note: The `nav` configuration already has the correct structure (Home: index.md, Ch1: ch1-ai-supported-software.md) per D-07. No changes needed to nav.
  </action>
  <verify>
    <automated>grep -q 'docs_dir: book/docs' book/mkdocs.yml && echo PASS</automated>
  </verify>
  <done>mkdocs.yml configured per D-04, D-05, D-06, D-07</done>
</task>

<task type="auto">
  <name>Task 3: Validate mkdocs build --strict</name>
  <files>book/mkdocs.yml</files>
  <read_first>
    - book/mkdocs.yml (just updated)
  </read_first>
  <action>
    Run `cd book && mkdocs build --strict` and verify it succeeds with exit code 0.

    Per D-09 and BUILD-01: The strict build must succeed with no warnings or errors.

    This validates that the docs_dir configuration is correct and all files are accessible.
  </action>
  <verify>
    <automated>cd book && mkdocs build --strict && echo PASS</automated>
  </verify>
  <done>mkdocs build --strict succeeds with no warnings or errors</done>
</task>

<task type="auto">
  <name>Task 4: Validate mkdocs serve</name>
  <files>book/mkdocs.yml</files>
  <read_first>
    - book/mkdocs.yml (just updated)
  </read_first>
  <action>
    Run `cd book && timeout 10 mkdocs serve` and verify it starts successfully.

    Per D-10 and BUILD-03: mkdocs serve must run for live preview during development.

    The serve command should bind to localhost:8000 and report "Serving on http://localhost:8000".
  </action>
  <verify>
    <automated>cd book && timeout 5 mkdocs serve 2>&1 | grep -q 'localhost:8000' && echo PASS</automated>
  </verify>
  <done>mkdocs serve runs and serves the documentation site on localhost:8000</done>
</task>

</tasks>

<verification>
- `cd book && mkdocs build --strict` exits with code 0
- `cd book && timeout 5 mkdocs serve 2>&1` shows "Serving on http://localhost:8000"
</verification>

<success_criteria>
- mkdocs.yml has docs_dir: book/docs
- mkdocs.yml has edit_url with correct GitHub repository path
- mkdocs.yml site_url and extra.homepage updated from placeholder
- mkdocs build --strict succeeds
- mkdocs serve starts successfully
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>