---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - book/.python-version
  - book/ai_supported_software_development_chapter.md
autonomous: true
requirements:
  - BUILD-03
must_haves:
  truths:
    - "Python 3.12 is pinned for local development and CI/CD"
    - "Stale chapter file is removed, only canonical ch1 exists in docs/"
  artifacts:
    - path: "book/.python-version"
      provides: "Python version pinning"
      content: "3.12"
    - path: "book/ai_supported_software_development_chapter.md"
      provides: "Stale file removal verification"
      state: "deleted"
  key_links:
    - from: "book/.python-version"
      to: "Cloudflare Pages build"
      via: "read by build system"
      pattern: "3.12"
    - from: "book/mkdocs.yml"
      to: "book/docs/"
      via: "docs_dir configuration"
      pattern: "docs_dir: book/docs"
---

<objective>
Create Python version file and clean up stale chapter file. These are independent tasks that establish the foundation for Phase 2 CI/CD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@book/mkdocs.yml
@book/ai_supported_software_development_chapter.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create book/.python-version with Python 3.12</name>
  <files>book/.python-version</files>
  <read_first>
    - book/.python-version (does not exist yet - no read needed)
  </read_first>
  <action>
    Create file `book/.python-version` with content `3.12` on a single line with no trailing newline.

    Per D-08: Pin Python to 3.12 via book/.python-version — aligns with MkDocs 1.6.x requirements and Cloudflare Pages support.

    The file should contain exactly:
    ```
    3.12
    ```
  </action>
  <verify>
    <automated>cat book/.python-version | grep -q '^3.12$' && echo PASS</automated>
  </verify>
  <done>book/.python-version created with content "3.12"</done>
</task>

<task type="auto">
  <name>Task 2: Delete stale chapter file</name>
  <files>book/ai_supported_software_development_chapter.md</files>
  <read_first>
    - book/ai_supported_software_development_chapter.md (stale file to be deleted)
    - book/docs/ch1-ai-supported-software.md (canonical ch1 file)
  </read_first>
  <action>
    Delete the stale chapter file `book/ai_supported_software_development_chapter.md`.

    Per Pitfall 1 from RESEARCH: This old Ch1 filename exists in book/ root alongside the canonical file in docs/. It creates confusion about which file is canonical and may be included in EPUB build.

    The canonical ch1 file is `book/docs/ch1-ai-supported-software.md` (per D-03). The stale file in book/ root must be removed.
  </action>
  <verify>
    <automated>ls book/ai_supported_software_development_chapter.md 2>/dev/null && echo FAIL || echo PASS</automated>
  </verify>
  <done>Stale chapter file deleted, canonical ch1 preserved in docs/</done>
</task>

</tasks>

<verification>
- `cat book/.python-version` outputs "3.12"
- `ls book/ai_supported_software_development_chapter.md 2>/dev/null` returns "No such file or directory"
- `ls book/docs/ch1-ai-supported-software.md` confirms canonical file exists
</verification>

<success_criteria>
- book/.python-version exists with content "3.12"
- book/ai_supported_software_development_chapter.md is deleted
- book/docs/ch1-ai-supported-software.md is preserved
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>