---
phase: 03-polish
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - book/build.sh
autonomous: true
requirements:
  - BUILD-02
user_setup: []
---

<objective>
Update book/build.sh to produce a working EPUB with only the files that currently exist (index.md + ch1). Remove references to non-existent ch2-ch6 files.
</objective>

<context>
@book/build.sh

## Non-existent files referenced in current build.sh

The current build.sh lists ch2 through ch6 which do not exist in book/docs/:
- ch2-agentic-workflows.md (does not exist)
- ch3-model-integration.md (does not exist)
- ch4-ai-driven-development.md (does not exist)
- ch5-production-guardrails.md (does not exist)
- ch6-conclusion.md (does not exist)

Existing files:
- index.md
- ch1-ai-supported-software.md
- ch7-context-engineering.md (exists but excluded from nav — out of launch scope)
- ch8-agentic-design-patterns.md (exists but excluded from nav — out of launch scope)
- ch9-what-is-agentic-design-pattern.md (exists but excluded from nav — out of launch scope)

Pandoc will fail if given files that do not exist. BUILD-02 requires the EPUB to build successfully.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update book/build.sh to reference only existing files</name>
  <files>book/build.sh</files>
  <read_first>
    - book/build.sh
  </read_first>
  <action>
    Replace the pandoc command in build.sh to reference only the files that currently exist and are in launch scope (Home + Ch1):

    pandoc \
      "$DOCS_DIR/index.md" \
      "$DOCS_DIR/ch1-ai-supported-software.md" \
      -o "$EPUB" \
      --from markdown --to epub3 \
      --split-level=1 \
      --toc --toc-depth=2 \
      --css="assets/epub.css" \
      --metadata title="From Exploration to Engineering" \
      --metadata author="Your Name" \
      --metadata lang="en"

    Changes from current:
    1. Remove ch2 through ch6 references (files do not exist)
    2. Add --split-level=1 (explicit chapter splitting)
    3. Add --toc --toc-depth=2 (table of contents)
    4. Add --metadata lang="en" (proper EPUB language tagging)

    The --css="assets/epub.css" is kept as-is since the file already exists at book/docs/assets/epub.css.
  </action>
  <verify>
    <automated>bash book/build.sh && unzip -l book/output/exploration-to-engineering.epub | grep -E '(nav|xhtml|opf|mimetype)'</automated>
  </verify>
  <done>book/build.sh runs without Pandoc file-not-found errors; book/output/exploration-to-engineering.epub exists and is non-empty; unzip -l shows nav.xhtml and chapter content.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "EPUB export produces readable file with chapters and code blocks"
  artifacts:
    - path: "book/output/exploration-to-engineering.epub"
      provides: "working EPUB with index.md and ch1 content"
      min_bytes: 1000
    - path: "book/build.sh"
      provides: "EPUB build script that references only existing files"
      contains: "index.md, ch1-ai-supported-software.md"
  key_links:
    - from: "book/build.sh"
      to: "book/output/exploration-to-engineering.epub"
      via: "pandoc --to epub3"
      pattern: "pandoc.*epub3"
</must_haves>

<verification>
- `bash book/build.sh` exits 0 with no Pandoc errors
- `ls -la book/output/exploration-to-engineering.epub` shows file size > 0
- `unzip -l book/output/exploration-to-engineering.epub` lists nav.xhtml, content OPF, mimetype
</verification>

<success_criteria>
- book/build.sh references only index.md and ch1-ai-supported-software.md
- book/build.sh adds --toc --toc-depth=2 and --metadata lang="en"
- `bash book/build.sh` produces book/output/exploration-to-engineering.epub without errors
- EPUB file is non-empty and contains chapter content
</success_criteria>

<output>
Create .planning/phases/03-polish/03-02-SUMMARY.md when done
</output>