---
phase: 02-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - book/mkdocs.yml
  - .gitignore
autonomous: true
requirements:
  - DEPLOY-04

must_haves:
  truths:
    - "mkdocs.yml has site_dir configured to output to repo root site/"
    - ".gitignore excludes site/ and book/site/ build artifacts"
  artifacts:
    - path: "book/mkdocs.yml"
      provides: "MkDocs configuration with site_dir for root-level output"
      contains: "site_dir: ../site"
    - path: ".gitignore"
      provides: "Git ignore rules excluding build output"
      contains: "site/"
  key_links:
    - from: "book/mkdocs.yml"
      to: "site/"
      via: "site_dir: ../site resolves to repo_root/site/"
    - from: ".github/workflows/deploy.yml"
      to: "site/"
      via: "wrangler-action deploys from site/ directory"
---

<objective>
Configure MkDocs to output build artifacts to the repo root `site/` directory, and update `.gitignore` to exclude build artifacts.

Purpose: The CI workflow builds from book/ directory but must deploy the output from repo root `site/`. The mkdocs.yml site_dir setting and .gitignore entries ensure correct artifact placement.

Output: Modified `book/mkdocs.yml` (adds site_dir) and modified `.gitignore` (adds site/ entries).
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-pipeline/02-CONTEXT.md
@.planning/phases/02-pipeline/02-RESEARCH.md
@book/mkdocs.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add site_dir to mkdocs.yml</name>
  <read_first>book/mkdocs.yml</read_first>
  <action>
Read book/mkdocs.yml, then add the line `site_dir: ../site` to it. This setting makes MkDocs output the built site to the repo root `site/` directory instead of `book/site/`.

The site_dir path is relative to the config file location (book/mkdocs.yml), so `../site` resolves to `repo_root/site/`.

Add this configuration immediately after the `docs_dir: docs` line so the relevant directory settings are grouped together.

Key constraint per D-08: site_dir must be `../site` relative to book/mkdocs.yml location.
  </action>
  <verification>
    <automated>grep -q "site_dir: ../site" book/mkdocs.yml && echo "PASS"</automated>
  </verification>
  <done>
    - `book/mkdocs.yml` contains `site_dir: ../site` (DEPLOY-04)
    - The path resolves to repo root `site/` when mkdocs build runs from book/ directory
  </done>
</task>

<task type="auto">
  <name>Task 2: Add site/ entries to .gitignore</name>
  <read_first>.gitignore</read_first>
  <action>
Read the existing `.gitignore` file, then append the following two lines:
```
site/
book/site/
```

These entries prevent the MkDocs build output directories from being committed to the repository. The CI builds fresh each time, so there is no need to track these artifacts in git.

Add these entries at the end of the file on new lines.
  </action>
  <verification>
    <automated>grep -q "^site/$" .gitignore && grep -q "^book/site/$" .gitignore && echo "PASS"</automated>
  </verification>
  <done>
    - `.gitignore` contains `site/` (line starting with site/)
    - `.gitignore` contains `book/site/` (line starting with book/site/)
  </done>
</task>

</tasks>

<verification>
- `grep -q "site_dir: ../site" book/mkdocs.yml` succeeds
- `grep -q "^site/$" .gitignore` succeeds
- `grep -q "^book/site/$" .gitignore` succeeds
</verification>

<success_criteria>
MkDocs is configured to output to repo root `site/` directory via `site_dir: ../site` in `book/mkdocs.yml`, and `.gitignore` excludes both `site/` and `book/site/` from version control.
</success_criteria>

<output>
Create `.planning/phases/02-pipeline/02-02-SUMMARY.md` when done
</output>